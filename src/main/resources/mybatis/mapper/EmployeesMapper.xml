<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.alex.demo.mybatis.mapper.EmployeesMapper">
  <resultMap id="BaseResultMap" type="com.alex.demo.mybatis.model.Employee">
    <result column="emp_no" jdbcType="INTEGER" property="empNo" />
    <result column="birth_date" jdbcType="DATE" property="birthDate" />
    <result column="first_name" jdbcType="VARCHAR" property="firstName" />
    <result column="last_name" jdbcType="VARCHAR" property="lastName" />
    <result column="gender" jdbcType="CHAR" property="gender" />
    <result column="hire_date" jdbcType="DATE" property="hireDate" />
  </resultMap>
  <insert id="insertEmployee" parameterType="com.alex.demo.mybatis.model.Employee">
    <selectKey keyProperty="empNo" order="AFTER" resultType="java.lang.Integer">
      SELECT max(emp_no) +1 from employees
    </selectKey>
    insert into employees (emp_no, birth_date, first_name, last_name, 
      gender, hire_date)
    values (#{empNo,jdbcType=INTEGER}, #{birthDate,jdbcType=DATE}, #{firstName,jdbcType=VARCHAR}, #{lastName,jdbcType=VARCHAR}, 
      #{gender,jdbcType=CHAR}, #{hireDate,jdbcType=DATE})
  </insert>
  
  <select id="selectAll" resultMap="BaseResultMap">
    select emp_no, birth_date, first_name, last_name, gender, hire_date
    from employees limit 20
  </select>
  
  <select id="findEmployeeById" parameterType="int" resultType="com.alex.demo.mybatis.model.Employee">
    select emp_no, birth_date, first_name, last_name, gender, hire_date
    from employees where emp_no=#{empNo}
  </select>
  
  <select id="findEmployees" parameterType="map" resultMap="BaseResultMap">
    select emp_no, birth_date, first_name, last_name, gender, hire_date
    from employees where 1=1
    <if test="empNo != '' and empNo != null">
      and emp_no = #{empNo}
    </if>
    <if test="firstName != '' and firstName != null">
      and first_name like concat('%',#{firstName},'%')
    </if>
    <if test="lastName != '' and lastName != null">
      and last_name like concat('%',#{lastName},'%')
    </if>
    limit 20
  </select>
  
  <update id="updateEmployee" parameterType="com.alex.demo.mybatis.model.Employee">  
        UPDATE employees  
            SET first_name = #{firstName},   
                last_name = #{lastName},  
                birth_date = #{birthDate},  
                gender = #{gender},
                hire_date = #{hireDate}
         WHERE emp_no = #{empNo}
	</update>
	
	<delete id="deleteEmployeeById" parameterType="int">  
	     DELETE FROM employees WHERE emp_no = #{empNo}
	</delete>  
  
</mapper>